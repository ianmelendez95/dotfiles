# Lines configured by zsh-newuser-install
HISTFILE=~/.histfile
HISTSIZE=1000
SAVEHIST=1000
setopt autocd beep extendedglob nomatch notify
bindkey -v
# End of lines configured by zsh-newuser-install
# The following lines were added by compinstall
zstyle :compinstall filename '/home/imelendez/.zshrc'

autoload -Uz compinit
compinit
# End of lines added by compinstall

[ -f "/home/imelendez/.ghcup/env" ] && . "/home/imelendez/.ghcup/env" # ghcup-env

# chezmoi
alias chez="chezmoi"
alias cheza="chezmoi add"
alias chezcd="chezmoi cd"
alias cheze="chezmoi edit"

# git
alias gs="git status"
alias gcm="git commit -m"

# setup_arch.sh
alias csetup="vim ~/arch-setup/setup_arch.sh"
alias ssetup="source ~/arch-setup/setup_arch.sh"

# zsh
alias cz="vim ~/.zshrc"
alias sz="source ~/.zshrc"
